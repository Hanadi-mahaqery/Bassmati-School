import 'package:flutter/material.dart';

class Total2Screen extends StatelessWidget {
  const Total2Screen({super.key});
  static String routeName = 'Total2Screen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(
            'Session 2 Total'
        ),
      ),
    );
  }
}
